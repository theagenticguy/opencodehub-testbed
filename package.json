{
  "name": "opencodehub-testbed",
  "version": "0.0.0",
  "private": true,
  "description": "Heavy evals, gyms, corpora, and bench harnesses for OpenCodeHub — consumes @opencodehub/cli@latest nightly.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "install:cli": "pnpm add -g @opencodehub/cli@latest",
    "test:eval": "cd eval && uv sync && uv run pytest src/opencodehub_eval/tests/test_parametrized.py -q",
    "gym:build": "cd gym && pnpm install && pnpm run build",
    "gym:run": "node gym/dist/cli.js run",
    "gym:replay": "node gym/dist/cli.js replay",
    "smoke:analyze": "codehub analyze gym/corpus/repos/typescript/ts-pattern"
  }
}
